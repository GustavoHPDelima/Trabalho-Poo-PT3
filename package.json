{
  "name": "biblioteca-afya",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "build:web": "tsc -p tsconfig.web.json",
    "start": "node dist/index.js",
    "test": "jest --coverage",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.6.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.5.0"
  }
}
